name: Build Nxtrive

on:
  push:
    tags: ["v*"]
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: nxtrive-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        include:
          - platform: macos-latest
            args: ""
            target_triple: aarch64-apple-darwin
            bundle_dir: src-tauri/target/release/bundle/
            artifact_label: macos-arm64
            python: "3.12"
          - platform: ubuntu-22.04
            args: --bundles deb,rpm
            target_triple: x86_64-unknown-linux-gnu
            bundle_dir: src-tauri/target/release/bundle/
            artifact_label: linux-x64
            python: "3.12"
          - platform: windows-latest
            args: ""
            target_triple: x86_64-pc-windows-msvc
            bundle_dir: src-tauri/target/release/bundle/
            artifact_label: windows-x64
            python: "3.12"

    runs-on: ${{ matrix.platform }}
    timeout-minutes: 90

    env:
      APPIMAGE_EXTRACT_AND_RUN: "1"
      NO_STRIP: "true"
      APPLE_SIGNING_IDENTITY: "-"

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libssl-dev \
            libayatana-appindicator3-dev librsvg2-dev patchelf libfuse2 fuse3 \
            squashfs-tools zsync file libglib2.0-dev libgdk-pixbuf2.0-dev binutils \
            desktop-file-utils

      - name: Build Python backend (Unix)
        if: matrix.platform != 'windows-latest'
        env:
          BACKEND_TARGET_TRIPLE: ${{ matrix.target_triple }}
        run: bash scripts/build-backend.sh

      - name: Build Python backend (Windows)
        if: matrix.platform == 'windows-latest'
        shell: pwsh
        run: .\scripts\build-backend.ps1

      - name: Install frontend deps
        run: npm ci --ignore-scripts

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPIMAGE_EXTRACT_AND_RUN: "1"
          NO_STRIP: "true"
        with:
          projectPath: .
          tagName: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}
          releaseName: Nxtrive ${{ github.ref_name }}
          releaseBody: |
            ## Nxtrive ${{ github.ref_name }}

            100% offline AI document chat for Windows, macOS (Apple Silicon), and Linux.

            | Platform | Installers |
            |----------|------------|
            | Windows | `.msi`, `.exe` (NSIS) |
            | macOS | `.dmg` (Apple Silicon) |
            | Linux | `.deb`, `.rpm` |
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

      - name: Upload installers
        uses: actions/upload-artifact@v4
        with:
          name: nxtrive-${{ matrix.artifact_label }}
          path: ${{ matrix.bundle_dir }}
          if-no-files-found: error
          retention-days: 30
